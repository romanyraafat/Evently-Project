import 'package:evently_project/core/utils/app_colors.dart';
import 'package:flutter/material.dart';

class AppTheme {
  // 🌞 Light Theme
  static ThemeData lightTheme = ThemeData(primaryColor: AppColors.primary);

  // 🌚 Dark Theme
  static ThemeData darkTheme = ThemeData(primaryColor: AppColors.primaryDark);
}
